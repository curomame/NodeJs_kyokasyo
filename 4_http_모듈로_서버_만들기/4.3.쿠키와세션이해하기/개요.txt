클라에서 보내는 요청에는 큰 단점이 있는데 그건 바로 누가 요청을 보내는지 모른다는 것이다.
여러 컴퓨터가 공통 ip가지거나 한사람이 여러 컴퓨터 사용할수도 있음
그래서 로그인을 구현해야하는데 그러기 위해서는 쿠키와 세션을 알아야함.

누구인지 알기 위해 서버는 요청에 대한 응답을 할 때 쿠키라는것을 같이 보냄
쿠키는 유효기간 있고 key-value 형식으로 쿠키쿠키!
서버로부터 쿠키 오면 웹 브라우저는 쿠키를 저장했다가 다음에 요청할때마다 쿠키 동봉해서 보냄서버는 요청에 들어있는
쿠키 읽어서 사용자 누구인지 파악

브라우저는 쿠키 있으면 자동으로 동봉해서 보내니까 따로 처리할 필요 없고
서버에서 브라우저로 보낼때만 코드 작성해서 처리할

서버는 클라에 요청자를 추정할만한 정보를 쿠키로 만들어 보내고 그다음부터는 클라로부터 쿠키 받아서 요청자 파악
쿠키는 요청 헤더에 담겨 전송되고 브라우저는 응답해서에 따라 쿠키를 저장
cookie - setCookie

